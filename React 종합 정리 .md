# React 종합 정리

# React 기본 개념

## 마크업 언어

**XML (eXtensible Markup Language)**

- 태그를 확장시킨 언어

**XHTML (eXtensible HTML)**

- HTML을 확장시킨 언어

## React란?

**정의**

- 복잡한 사용자 인터페이스(UI)를 쉽게 구축하기 위해 페이스북에서 제작된 자바스크립트 기반의 라이브러리
- 싱글 페이지 애플리케이션이나 모바일 애플리케이션 개발에 주로 사용된다

**특징**

- 특정 데이터에 변화가 생길 때 매번 변화를 주기 위한 코드를 작성하는 것이 아니라, 기존 코드를 없애고 다시 새롭게 랜더링한다

## 랜더링(Rendering)

**랜더링이란?**

- 화면에 보여주는 것
- 코드가 번역(컴파일)되어 실행된 뒤 화면을 통해 사용자에게 보여진다

### 초기 랜더링(Mount)

- 가장 처음으로 랜더링 되는 것
- `render()` 함수를 사용하여 view를 정의하고 초기 랜더링을 진행하게 된다

### 리랜더링(Re-rendering)

- 뷰를 업데이트 해야 할 때 다시 지웠다가 그려야하며, 이를 리랜더링이라고 한다
- 전체를 모두 지우고 다시 그리는 것은 비효율적이므로, 이전의 DOM 트리와 새로운 DOM 트리를 비교하여 변경된 요소만 리랜더링한다

**리랜더링 규칙**

- 부모가 리랜더링되면 자식도 리랜더링 된다
- 반대로 자식이 리랜더링 된다면, 부모가 리랜더링 될 수도 있다

## 컴포넌트(Component)

**정의**

- 리액트에서 앱을 이루는 최소한의 단위를 의미한다
- 컴포넌트가 모여서 하나의 앱이 되며, 각 컴포넌트는 분리되어 재사용이 가능하다

## 버츄얼 돔(Virtual DOM)

**동작 방식**

- 화면보다 가상 돔에 먼저 랜더링 된다
- 이전 Virtual DOM과 현재 Virtual DOM을 비교하여 변경이 필요한 부분만 리랜더링 시킨다

**특징**

- React가 가지고 있는 가상 돔의 복사본이다
- state(상태)의 변화를 React에서 감지하고 있다가 state의 변화가 생기면 해당 state를 가지고 있는 components의 가상 돔과 실제 돔을 비교하여 변화가 생긴 components만 리랜더링 한다

## JSX 주의사항

**폴더 구조**

- 폴더를 열었을 때 `index`가 있을 시 index가 대장 역할을 한다

**DOM 트리 규칙**

- DOM 트리는 하나로 만들어야 한다
- 두 개일 시 에러 발생

---

# 리액트 라이프사이클

## 🔄 리액트 라이프사이클

페이지에 컴포넌트가 **생성되고, 수정되고, 사라지는 순서**를 의미합니다.

React에서 클래스 컴포넌트를 사용하면, 9개의 메소드를 통해 작업 흐름을 제어할 수 있습니다.

## 📌 마운트 (Mount)

- **constructor** : 새로운 컴포넌트를 생성할 때마다 실행
- **getDerivedStateFromProps** : props를 state에 넣을 때 사용. 컴포넌트가 마운트될 때와 업데이트가 될 때 실행
- **render** : 준비해놓은 UI를 렌더링할 때 실행
- **componentDidMount** : 페이지에 컴포넌트가 나타난 직후 실행

## 🔁 업데이트 (Update)

- **getDerivedStateFromProps**
- **shouldComponentUpdate** : `true`를 리턴 시 다음 라이프사이클 메소드가 계속 실행. `false`를 리턴할 경우 작업이 중지
- **getSnapshotBeforeUpdate** : 컴포넌트 업데이트 직전의 값을 snapshot에 저장. 업데이트 직전에 실행
- **componentDidUpdate** : 컴포넌트 업데이트 직후 실행

## 🗑️ 언마운트 (Unmount)

- **componentWillUnmount** : 컴포넌트가 사라지기 직전에 실행되는 메서드

---

# 사이드 이팩트 제어하기

## ⚠️ 사이드 이팩트 제어하기

### 📖 참조 투명성 (Referential Transparency)

> 리액트는 모든 함수를 순수 함수로 유지할 것을 권장합니다. 이를 통해 컴포넌트의 재사용성이 좋아지고 오류 검사 및 테스트도 용이해집니다.
> 

**순수 함수 (Pure Function)**

- 입력을 받아서 입력받은 그대로를 출력하는 함수
- **참조 투명**한 함수

**비순수 함수 (Impure Function)**

- 입력을 받았을 때 다른 결과로 출력하거나 외부 함수에 영향을 끼치는 함수
- **참조 불투명**한 함수
- 이러한 함수에서 **Side-Effect**가 발생

### 💥 Side-Effect란?

함수 내 특정 동작이 함수 외부에 영향을 끼쳐, 프로그램의 동작을 이해하기 어렵게 만드는 행위

**예시**

- `setTimeout`, `setInterval`
- `fetch`로 서버와의 통신
- DOM 직접 조작 등

### 🎯 useEffect Hook

Side-Effect를 따로 관리하여 컴포넌트가 최대한 순수 함수를 유지할 수 있도록 도와주는 Hook 함수

**사용 방법**

- `useEffect(() => {})`
    
    렌더링될 때마다 실행
    
- `useEffect(() => {}, [])`
    
    최초 마운트가 됐을 때 한 번만 실행 (= `componentDidMount`)
    
- `useEffect(() => {}, [state])`
    
    의존성 배열의 상태가 바뀌었을 때마다 실행
    

---

# Memoization

## 🚀 Memoization

함수형 컴포넌트에서 다른 변화 때문에 발생하는 **불필요한 연산을 방지**하기 위해 사용합니다.

### 💾 useMemo란?

**useMemo**는 메모리에 있는 캐시를 사용하는 함수입니다.

**동작 원리**

1. 최초 마운트 시 연산 결과를 캐시에 저장
2. 리렌더링 발생 시 캐시와 결과를 비교
3. 의존성 값의 변화가 있을 때만 연산을 수행

이러한 방식을 **Memoization**이라고 합니다.

---

# Context API

## 🌐 Context API

리액트의 내장 API(16버전 이상) 중 하나로, **Props를 넘겨주지 않고도** 컴포넌트 트리 전체에 데이터를 처리할 수 있도록 제공하는 API입니다.

### 🔽 Props Drilling이란?

A 컴포넌트부터 Z 컴포넌트까지 부모에서 최하위 자식까지 Props를 전달하는 과정을 **Props Drilling**이라고 합니다.

### 🎨 Context API 사용 시점

**적합한 경우**

- 변화가 없는 데이터 (다크모드/라이트모드 등)
- 값이 선언되어 있는 상태로 변화가 없는 변수

> **주의**: Context는 변화를 시켜주는 API가 아닙니다. 변화를 주면 안 되지만, 혹시나 변화가 생길 것을 대비하여 만들어져 있습니다.
> 

### ⚠️ Context의 단점

- **Provider 컴포넌트**는 `value` Props를 받아서 하위 컴포넌트에게 전달
- `value`가 업데이트되면 **Context로 감싸진 모든 컴포넌트가 리렌더링**됨
- 성능 이슈 발생 가능

---

# Routing과 렌더링 방식

## Routing이란?

클라이언트가 요청한 URL을 분석하여 알맞은 페이지로 응답하는 것으로, 여러 페이지로 구성된 웹 애플리케이션을 만들 때 사용합니다.

---

## MPA (Multiple Page Application)

여러 개의 페이지로 이루어진 애플리케이션입니다.

**특징**

- 클라이언트가 서버에 요청을 보내면 서버에서 렌더링한 뒤 클라이언트에 응답하는 **SSR(Server Side Rendering)** 방식을 사용
- 페이지 이동 시마다 새로고침이 발생하여 깜빡임 현상이 나타남

---

## SPA (Single Page Application)

하나의 페이지로 이루어진 애플리케이션입니다.

**특징**

- 페이지를 딱 한 개만 받아온 뒤 데이터만 받아와서 화면을 업데이트
- 링크 클릭 시 경로가 바뀌며 히스토리에도 남아 클라이언트 입장에서는 여러 페이지가 존재하는 것처럼 보임
- 경로가 바뀌어도 주소창의 경로만 변경되고 기존 페이지는 유지하며 다른 페이지의 데이터를 보여줌
- **CSR(Client Side Rendering)** 방식을 사용
- 페이지 이동 속도가 굉장히 빠름

**단점**

- 비어있는 HTML 문서가 도착한 뒤 JavaScript 연산 및 데이터 요청이 시작되므로 데이터 분석과 검색 엔진에 취약

---

## CSR vs SSR 비교

### CSR (Client Side Rendering)

**장점**

- 페이지에 필요한 리소스를 전부 사전에 불러와 데이터를 캐싱하고 있다가 현재 URL에 맞는 페이지를 보여주기 때문에 초기 렌더링 이후 속도가 빠름

**단점**

- 모든 리소스를 불러와야 하기 때문에 초기 페이지 로딩이 SSR보다 느림
- SEO(검색 엔진 최적화)에 불리함

### SSR (Server Side Rendering)

**장점**

- SEO에 유리함 (서버에서 완성된 페이지를 전달해주기 때문에 검색 엔진이 수집하기 용이)
- 서버에서 렌더링을 부담하기 때문에 사용자의 하드웨어에 의존하지 않음

**단점**

- 서버에 부담이 되어 생산 비용이 증가함
- SSR을 위한 별도의 코드 작성이 필요함

---

# Zustand & React Hook Form

## Zustand

### 개요

전역 상태를 간단하고 효율적으로 관리할 수 있게 도와주는 상태 관리 라이브러리입니다. 메모리 기반 상태 관리를 제공하며, 상태가 브라우저 메모리(RAM)에 저장되어 앱이 살아 있는 동안만 유지됩니다. 상태 유지를 위해 주로 로컬스토리지와 함께 사용됩니다.

📚 [공식 문서](https://zustand.docs.pmnd.rs/getting-started/introduction)

### Store란?

컴포넌트 간의 데이터를 공유하기 위해 상위 컴포넌트를 거쳐야 하는 **Props Drilling**을 없애고, 컴포넌트 간에 Store를 사이에 두어 여러 컴포넌트가 같은 데이터를 쉽게 보고 수정할 수 있는 중앙 저장소를 의미합니다.

### 주요 함수

#### `create`

- State와 Action을 통합 정의할 수 있습니다
- 반환되는 훅으로 상태 접근과 액션 호출을 동시에 처리할 수 있습니다
- 필요한 상태만 선택적으로 구독(subscribe)해 불필요한 리렌더링을 방지할 수 있습니다

#### `set`

- 상태를 업데이트하는 함수로, `create` 함수 내부에서 사용됩니다
- 기존 상태를 불변성을 유지하면서 부분적으로 변경할 수 있습니다
- 함수 형태로도 값을 갱신할 수 있어 복잡한 상태 계산도 가능합니다

#### `persist`

- Zustand 상태를 브라우저의 로컬 스토리지(localStorage)나 세션 스토리지(sessionStorage)에 자동으로 저장하고 불러올 수 있게 해주는 미들웨어입니다
- 페이지를 새로고침해도 상태가 유지됩니다
- 로그인 정보나 테마 설정 등 지속적인 상태 관리에 유용합니다

---

## React Hook Form

### 주요 API

#### `register`

input 요소를 React Hook Form과 연결해 검증 규칙을 적용할 수 있게 하는 메소드입니다.

#### `handleSubmit`

form을 submit했을 때 실행할 함수입니다.

#### `formState`

form state에 관한 정보를 담고 있는 객체입니다.

#### `errors`

input 값들의 에러 정보를 가지고 있는 객체입니다.

#### `getValues`

input 값을 가져올 수 있는 함수입니다.

### 검증 모드 설정

```jsx
useForm({ mode: "onChange" })
```

mode를 설정함으로써 해당 모드의 register 안 검증 로직이 동작하는 규칙을 정할 수 있습니다.

#### 모드 종류

- **`onChange`** : input 값이 바뀔 때마다 검증 로직이 동작합니다
- **`onBlur`** : 포커스 상태를 잃어버릴 때 동작합니다
- **`onSubmit`** : 제출 함수가 실행될 때 동작합니다
- **`onTouched`** : 첫 번째 blur 이벤트에서 동작하고, 그 후에는 모든 change 이벤트에서 동작합니다
- **`all`** : blur 및 change 이벤트에서 동작합니다

---

## 정규표현식 (Regular Expression)

정규표현식은 일종의 문자를 표현하는 공식으로, 특정 규칙이 있는 문자열 집합을 추출할 때 자주 사용되는 기법입니다.

### 수량자 (Quantifiers)

- `?` : 없거나 있거나 (zero or one)
- `*` : 없거나 많거나 (zero or more)
- `+` : 하나 또는 많이 (one or more)
- `{n}` : n개가 있는
- `{min}` : 최소
- `{min, max}` : 최소 그리고 최대

### 범위 (Group and Range)

- `()` : 그룹
- `[]` : 문자셋, 괄호 안에 어떤 문자
- `[^]` : 부정 문자셋
- `(?:)` : 찾지만 기억하지는 않음
- `(?=)` : 앞쪽과 일치
- `(?<=)` : 뒤쪽과 일치

---

# React Route & Style

## Custom Hooks Pattern 구조

## React 개발 원칙

React를 사용할 때 선언은 어렵게, 사용은 쉽게

수정을 고려하여 코드를 작성하기

## 라우터 설치

`yarn add react-router-dom`

## 프로젝트 폴더 구조

### [components]

모듈화 및 관심사의 분리

### [utils]

- arrayheler.js
- parseheler.js

### [consts]

상수 관리 폴더

- key.js - 상수, 문자열

### [styles]

- theme.js - 스타일 정의 및 가이드 컬러
- globalStyle.js - 전역 스타일 (reset, font-family)
- common.js - 공통 스타일

### [pages]

- Home
- Hooks
- Components
- home.js

### [routes]

라우팅 관리

### [hooks]

커스텀 훅 관리

---

## CSS 스타일링 방법

### React에서 CSS 사용

React에서 기존 CSS 방법 사용 가능

### 스타일 파일 구성

- **global.js** - 전역 스타일 (reset, font-family)
- **common.js** - 공통 스타일
- **theme.js** - 스타일 정의 가이드 컬러

### 주의사항

템플릿 리터럴 `${}` 사용 시 CSS 문법에서 세미콜론 `;` 잘 닫기